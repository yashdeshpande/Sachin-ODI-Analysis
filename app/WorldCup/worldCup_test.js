'use strict';

describe('careerAnalysisApp.worldCup module', function() {

  beforeEach(module('careerAnalysisApp.worldCup'));

  describe('worldCup controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var worldCupCtrl = $controller('worldCupCtrl');
      expect(worldCupCtrl).toBeDefined();
    }));

  });
});