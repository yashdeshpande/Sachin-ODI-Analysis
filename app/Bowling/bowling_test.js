'use strict';

describe('careerAnalysisApp.bowling module', function() {

  beforeEach(module('careerAnalysisApp.bowling'));

  describe('bowling controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var bowlingCtrl = $controller('bowlingCtrl');
      expect(bowlingCtrl).toBeDefined();
    }));

  });
});