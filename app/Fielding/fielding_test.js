'use strict';

describe('careerAnalysisApp.fielding module', function() {

  beforeEach(module('careerAnalysisApp.fielding'));

  describe('fielding controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var fieldingCtrl = $controller('fieldingCtrl');
      expect(fieldingCtrl).toBeDefined();
    }));

  });
});